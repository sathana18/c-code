#include <stdio.h>

int addNumbers(int a, int b) {
    int sum = a + b;
    return sum;
}

int main() {
    int num1 = 5, num2 = 10, sum;
    sum = addNumbers(num1, num2);
    printf("Sum of %d and %d is %d\n", num1, num2, sum);
    return 0;
}
