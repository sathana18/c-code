#include <stdio.h>
void temp(int x,int y)
{
    int temp = x;
    x=y;
    y=temp;
}
    int main()
{
    int a=4;
    int b=8;
    
    printf("%d %d \n",a,b);
     
    swap (a,b);
    
    printf("%d %d",a,b);
    return 0;
}
