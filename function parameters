#include <stdio.h>

void greet(char name[]);

int main() {
    char name[20];
    printf("Please enter your name: ");
    scanf("%s", name);
    greet(name);
    return 0;
}

void greet(char name[]) {
    printf("Hello, %s!\n", name);
}
